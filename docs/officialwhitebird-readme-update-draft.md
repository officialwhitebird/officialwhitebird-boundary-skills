# officialwhitebird README Update Draft

Status: draft only. Do not apply this file directly.

When the repository is published and the owner approves the profile update, add this row to the `Selected Tools` table in the `officialwhitebird` profile repository:

```markdown
| [officialwhitebird-boundary-skills](https://github.com/officialwhitebird/officialwhitebird-boundary-skills) | Run Boundary Pack | Routes AI-run boundary checks across handoff-lint, policy-lint, contract-lint, and claim-lint with a local case study |
```

Apply rule for the profile repository:

```bash
git add README.md
```

Do not use `git add -A` in the profile repository during this update.

